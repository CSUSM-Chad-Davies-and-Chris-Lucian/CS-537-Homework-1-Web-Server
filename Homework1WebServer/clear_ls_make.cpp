#include <cstdlib>
#include <stdio.h>
#include <unistd.h>

int main(int argc, char *argv[]) {
    system("clear");
    system("ls");
    system("echo ");
    system("make");
    system("echo ");
    return 0;
}
